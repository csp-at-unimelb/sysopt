
def ode_builder(
        inputs,
        parameters,
        function,
        initial_state,
        name=None
):
    # ouptut size is from initial state.
     raise NotImplemented


def function_wrapper(inputs, outputs, parameters, function, name=None):
    raise NotImplemented
